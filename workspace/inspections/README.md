# Inspections

Survey (`.survey.md`), inspection (`.inspect.md`), plan (`.plan.md`), and workflow
(`.workflows.md`) outputs generated by the `griptape-nodes-e2e-survey`,
`griptape-nodes-e2e-inspect`, and `griptape-nodes-e2e-plan` skills. See `../AGENTS.md` for the
workflow.
